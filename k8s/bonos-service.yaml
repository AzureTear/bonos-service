apiVersion: apps/v1
kind: Deployment
metadata:
  name: bonos-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bonos-service
  template:
    metadata:
      labels:
        app: bonos-service
    spec:
      containers:
      - name: bonos-service
        image: 742075245664.dkr.ecr.us-east-1.amazonaws.com/bonos-service:latest
        ports:
        - containerPort: 8004
        envFrom:
        - secretRef:
            name: casino-secrets
        livenessProbe:
          httpGet:
            path: /livez
            port: 8004
          initialDelaySeconds: 30
          periodSeconds: 15
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8004
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 5
        resources:
          requests:
            cpu: "100m"
          limits:
            cpu: "300m"
---
apiVersion: v1
kind: Service
metadata:
  name: bonos-service
spec:
  selector:
    app: bonos-service
  ports:
  - port: 8004
    targetPort: 8004
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: bonos-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: bonos-service
  minReplicas: 2
  maxReplicas: 6
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
